import { View, Text } from "react-native";
import React from "react";

const ItemSeperatorComponent = ({ gap = 20 }: any) => {
  return <View style={{ height: gap }}></View>;
};

export default ItemSeperatorComponent;
